-- You can add your own plugins here or in other files in this directory!
--  I promise not to create any merge conflicts in this directory :)
--
-- lazy.nvim's `{ import = 'custom.plugins' }` (see init.lua) automatically
-- picks up every `.lua` file in this directory that returns a plugin spec.
return {}
